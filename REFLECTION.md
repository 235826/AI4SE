# PatchPilot 反思报告

PatchPilot 的起点是一个相对模糊的目标：做一个能帮助修复测试问题的 coding agent，但不能把真实模型调用、文件修改和命令执行混成不可验证的黑盒。Superpowers 的价值首先体现在把这个目标拆成可讨论的工程对象。brainstorming 阶段先收敛了 v1 的边界：Python 与 pytest、默认离线运行、真实 OpenAI 仅为可选提供方、写入与命令都受护栏约束。随后 SPEC 将用户故事、功能规约、数据模型、风险和验收条件写成可检查的陈述；PLAN 再把它们拆为有依赖关系的 Task 1-13，并为每个任务给出文件、接口、失败测试、最小实现、通过验证和提交。这个过程的关键不是文档数量，而是让“智能修复”变成一组可观察的机制：动作如何表示、工具如何调度、失败反馈如何保存、何时停止、什么操作必须拒绝。

TDD 在 agentic software engineering 中提供的是行为约束，而不只是质量口号。普通开发者可能凭经验在实现后补测试，代理却更容易因为上下文窗口、生成惯性或表面合理的方案而越过接口边界。先写失败测试并记录红灯，要求实现者明确一个外部可见的契约；最小实现通过后再处理边界情形，则限制了无关重构和“顺手扩展”。在本项目中，路径穿越、绝对路径、危险 shell 命令、补丁行尾、pytest nodeid 解析、未知 action 和停止原因都被拆为独立测试点。这样测试既检验最终输出，也限定代理可以宣称完成的条件。TDD 并不能证明模型推理正确，更不能替代人工的安全判断，但它能把不稳定的生成过程固定在可重复执行的反馈回路内。

MockLLM 是这个回路能够可靠验收的核心，而非真实模型的低配替身。Harness 首先要验证的是输入、决策、动作、工具执行、反馈、记忆和停止策略之间的连接是否正确。若直接依赖在线模型，网络、配额、提示词微调、服务版本和随机输出都会让失败来源不清楚；测试通过也无法说明是架构正确还是恰好得到一次合适回答。MockLLM 用预设 Action 序列驱动 AgentLoop，使 run_tests、apply_patch、未知动作和达到最大轮次等路径都有确定结果。它让 CLI 默认演示可离线运行，也让 CI 和 Docker 构建不需要供应商凭据。真实 OpenAI provider 因而被放在明确的可选边界上：它用于扩展能力，但不被允许成为机制测试的前提。

凭据治理和分发同样是功能设计的一部分。CredentialManager 从环境变量或系统 keyring 获取值，避免把密钥写进源码、测试样例、日志或镜像层；README 只说明变量名和配置步骤，不给出可误复制的真实值。Dockerfile、`.dockerignore` 和 CI 共同决定哪些文件进入构建上下文、镜像和自动化环境，因此它们也是凭据边界。可复现的 `make test`、默认 mock、最小运行时镜像和 CI 构建使项目在没有密钥时仍可被安装、审阅和验证。这里的工程价值是降低协作和交付的信任成本：使用者可以先确认安全的本地机制，再自行选择是否配置外部提供方，而不是被迫把敏感信息交给每一次测试。

AI 提议并非全部照单全收。被采纳的是将领域模型、guardrail、feedback、memory、LLM provider、dispatcher 和 loop 分层的建议，因为这些边界直接对应测试与风险。被修改的是真实 OpenAI 的接入方式：最初的抽象只为离线测试预留错误路径，后续才在 CLI 中加入显式 provider 选择和凭据读取，保持 mock 为默认值。对路径检查、命令白名单和补丁行尾处理的建议也经过了补充：只检查字符串前缀或只允许一个测试命令不足以抵御路径归一化、shell 分隔符和跨平台文本变化，因此增加了更具体的测试。被拒绝的是在 v1 引入自动提交、无约束 shell、默认联网模型、复杂多代理编排和为展示而加的界面；它们扩大了安全面和不确定性，却不能提高本次 harness 验收的证据质量。

现有方法论仍有明显成本。冷启动时，读取技能说明、SPEC、PLAN 和历史提交需要时间，且文档过长会让实现者难以快速找到当前任务的精确接口。技能流程带来了有价值的红绿证据和最终验证，但对小改动也可能形成额外的文件、命令和状态维护；其收益依赖于任务风险，不能机械套用。subagent 更有上下文断裂风险：独立任务容易只满足局部测试，忽略上游约束、已有脏工作区或后续集成语义，因此必须通过明确的 Interfaces、提交哈希、AGENT_LOG 和最终全量验证重新建立证据链。冷启动验证已暴露过计划格式和断言表达需要修订，这说明文档不是一次性产物。后续迭代应继续压缩重复说明、为高风险策略增加性质测试和真实 provider 的受控集成测试，并定期用新的代理从 SPEC/PLAN 冷启动，验证这些文字仍能独立指导正确实现。
